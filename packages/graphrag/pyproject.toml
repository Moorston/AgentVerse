[project]
name = "agentverse-graphrag"
version = "0.1.0"
description = "AgentVerse GraphRAG engine"
requires-python = ">=3.12"
dependencies = [
    "pgvector>=0.3",
    "asyncpg>=0.30",
    "agentverse-graph-core",
    "agentverse-shared",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
sources = ["src"]

[tool.hatch.build.targets.wheel]
packages = ["src/agentverse/graphrag"]